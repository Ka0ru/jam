<h1>Ingredients list (ingredients#index)</h1>
<p>Find me in app/views/ingredients/index.html.erb</p>


<ul>
	<%= @ingredients.each do |ingredient| %>
		<li>
			<%= link_to ingredient.name, ingredient_url(ingredient) %>

			<%= link_to 'Edit', edit_ingredient_url(ingredient) %>

			<%= link_to 'Delete ingredient', ingredient_url(ingredient), :confirm => 'Are you sure ?', :method => :delete %> <br />

			Price : <%= ingredient.price %>€
		</li>
	<% end %>
</ul>