<h1>Products list</h1>
<!-- <p>Find me in app/views/products/index.html.erb</p> -->

<% if user_signed_in? %>
	<ul>
		<%= @products.each do |product| %>
			<li>
				<%= link_to product.name, product_url(product) %>

				<%= link_to 'Edit', edit_product_url(product) %>

				<%= link_to 'Delete product', product_url(product), :confirm => 'Are you sure ?', :method => :delete %> <br />

				Price : <%= product.price %>€
			</li>
		<% end %>
	</ul>
<% end %>